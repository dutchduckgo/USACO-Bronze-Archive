/*
public class eightQueens {
    static boolean[][] board;
    public static void main (String[] args) {
        board = new boolean[8][8];
        int queensLeft = 8;
        int rslt = 0;
        for (int i = 0; i < 8; i ++) {
            for (int j = 0; j < 8; j ++) {
                if (queensLeft <= 0) break;
                board[i][j] = true;
                queensLeft --;
                if (checkSol(board)) rslt ++;
            }
        }

        System.out.println(rslt);
    }
    private static Boolean checkSol (boolean[][] board) {
        int
        for (int i = 0; i < 8; i ++) {
            if (board[0][i]) {
                queenX =
            }
        }
    }
}
*/
